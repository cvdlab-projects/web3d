function Figure(type){
    this.type=type;
    this.points=new Array();
}

function getPoints(){
    return this.getPoints();
}

function getType(){
    return this.type;
}

function addPoint(p){
    this.points.add(p);
}